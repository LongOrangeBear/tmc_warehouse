# 🚀 Быстрый старт TMC Warehouse

## ✅ Установка завершена

Установлены:
- ✅ Tesseract OCR 5.3.4 (с русским и английским языками)
- ✅ Poppler utils (для работы с PDF)

## 🧪 Тестирование (прямо сейчас)

### Запустить все тесты:
```bash
cd /home/meow/work/tmc_warehouse
export PYTHONPATH=$PYTHONPATH:$(pwd)
.venv/bin/pytest tests/ -v
```

**Результат:** ✅ 23/23 теста проходят

---

## 🏃 Запуск системы

### 1. Запустить сервер (Терминал 1):
```bash
cd /home/meow/work/tmc_warehouse
./run_server.sh
```

Сервер запустится на: `http://127.0.0.1:8000`

**Swagger UI:** http://127.0.0.1:8000/docs

### 2. Запустить клиент (Терминал 2):
```bash
cd /home/meow/work/tmc_warehouse
./run_client.sh
```

---

## 🌐 Проверка API (через браузер или curl)

```bash
# Health check
curl http://127.0.0.1:8000/api/v1/health

# Список товаров
curl http://127.0.0.1:8000/api/v1/products

# Создать приёмку
curl -X POST http://127.0.0.1:8000/api/v1/receptions \
  -H "Content-Type: application/json" \
  -d '{
    "ttn_number": "TEST-001",
    "ttn_date": "2025-11-30",
    "supplier": "Тестовый поставщик",
    "items": [{
      "article": "BOLT-M10",
      "name": "Болт М10",
      "quantity": 100,
      "unit": "шт",
      "suspicious_fields": []
    }]
  }'
```

---

## 📱 Что работает в клиенте

✅ **Работает:**
- Подключение к серверу (статус в статус-баре)
- История приёмок
- OCR распознавание документов (PDF и изображения)
- Редактирование результатов OCR
- Создание приёмок
- Синхронизация с сервером

⚠️ **Ограничения:**
- Камера: работает только если подключено физическое USB устройство
- Без камеры входной контроль всё равно можно пройти (без видеозаписи)

---

## 📚 Документация

- **README.md** - Полная инструкция
- **docs/API.md** - API документация
- **docs/ARCHITECTURE.md** - Архитектура системы
- **Swagger UI** - http://127.0.0.1:8000/docs (когда сервер запущен)

---

## 🐙 GitHub готов

Создано:
- `.gitignore` - исключает БД, venv, логи
- `.github/workflows/ci.yml` - автотесты в GitHub Actions

**Можно пушить в GitHub!**

---

## 🎯 Типичные сценарии

### Сценарий 1: Тестирование API
1. `./run_server.sh`
2. Открыть http://127.0.0.1:8000/docs
3. Попробовать создать приёмку через Swagger UI

### Сценарий 2: Полный цикл (без камеры)
1. `./run_server.sh` (Терминал 1)
2. `./run_client.sh` (Терминал 2)
3. В клиенте: "Принять ТМЦ" → выбрать документ → OCR → создать
4. "История" → увидеть созданную приёмку

### Сценарий 3: Разработка
```bash
# Запустить только тесты БД
pytest tests/test_server_db.py -v

# Запустить тесты API
pytest tests/test_server_api.py -v

# Запустить сервер в dev режиме (с auto-reload)
./run_server.sh
```
