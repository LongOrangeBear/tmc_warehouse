# 🚀 Полная инструкция: Загрузка на GitHub и активация Portable Build

## 📋 Содержание

1. [Проверка Git](#1-проверка-git)
2. [Подготовка файлов](#2-подготовка-файлов)
3. [Создание репозитория на GitHub](#3-создание-репозитория-на-github)
4. [Загрузка кода](#4-загрузка-кода)
5. [Активация GitHub Actions](#5-активация-github-actions)
6. [Проверка сборки](#6-проверка-сборки)
7. [Скачивание результата](#7-скачивание-результата)
8. [Создание релиза](#8-создание-релиза-опционально)

---

## 1. ПРОВЕРКА GIT

### Шаг 1.1: Проверить установку Git

```bash
git --version
```

**Ожидаемый результат**: `git version 2.x.x`

**Если не установлен**:
```bash
# Ubuntu/Debian:
sudo apt install git

# Windows:
# Скачать с https://git-scm.com/downloads
```

---

### Шаг 1.2: Настроить Git (если первый раз)

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"
```

---

### Шаг 1.3: Проверить статус проекта

```bash
cd /home/meow/work/tmc_warehouse
git status
```

**Варианты ответа**:

- ✅ `On branch main` — репозиторий уже инициализирован
- ❌ `fatal: not a git repository` — нужно инициализировать (перейти к Шагу 1.4)

---

### Шаг 1.4: Инициализировать Git (если нужно)

```bash
cd /home/meow/work/tmc_warehouse
git init
git branch -M main
```

---

## 2. ПОДГОТОВКА ФАЙЛОВ

### Шаг 2.1: Проверить .gitignore

Убедитесь, что файл `.gitignore` существует и содержит:

```bash
cat .gitignore
```

**Должно быть**:
```
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
*.egg-info/

# IDEs
.idea/
.vscode/
*.swp
*.swo

# Data
data/
*.db
*.sqlite
*.log

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
```

**Если нет** — файл уже создан в проекте, но проверьте что data/ и .env в списке!

---

### Шаг 2.2: Добавить новые файлы workflow

```bash
git add .github/workflows/build-portable.yml
git add PORTABLE_BUILD_GUIDE.md
git add DEPLOYMENT_CHECKLIST.md
git add requirements-dev.txt
```

---

### Шаг 2.3: Проверить что будет загружено

```bash
git status
```

**Должно показать**:
```
Changes to be committed:
  new file:   .github/workflows/build-portable.yml
  new file:   DEPLOYMENT_CHECKLIST.md
  new file:   PORTABLE_BUILD_GUIDE.md
  new file:   requirements-dev.txt
```

**⚠️ НЕ должно быть**:
- ❌ `data/` (база данных)
- ❌ `.venv/` (виртуальное окружение)
- ❌ `__pycache__/`
- ❌ `.env` (секреты)

**Если есть лишнее**:
```bash
git reset HEAD data/
git reset HEAD .venv/
git reset HEAD .env
```

---

### Шаг 2.4: Создать коммит

```bash
git commit -m "feat: add portable Windows build workflow

- Add GitHub Actions workflow for fully automated portable build
- Include Tesseract OCR and Poppler bundled
- Add deployment guides and checklists
- Add requirements-dev.txt for build dependencies"
```

---

## 3. СОЗДАНИЕ РЕПОЗИТОРИЯ НА GITHUB

### Вариант А: У вас УЖЕ есть репозиторий на GitHub

**Пропустите к [Шаг 4](#4-загрузка-кода)**

---

### Вариант Б: Создать НОВЫЙ репозиторий

#### Шаг 3.1: Войти на GitHub

Откройте браузер: https://github.com

**Если нет аккаунта** → Sign up (создать бесплатный)

---

#### Шаг 3.2: Создать новый репозиторий

1. **Нажать** зеленую кнопку **"New"** (или перейти на https://github.com/new)

2. **Заполнить**:
   - **Repository name**: `tmc_warehouse` (или любое имя)
   - **Description**: `TMC Warehouse System - автоматизация складского учёта`
   - **Visibility**: 
     - ✅ **Private** (если только для вас)
     - ⬜ Public (если планируете открыть)
   - **⚠️ НЕ отмечать**:
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
     (они у вас уже есть локально!)

3. **Нажать** "Create repository"

---

#### Шаг 3.3: Скопировать URL репозитория

GitHub покажет страницу с инструкциями. **Скопируйте** URL вида:
```
https://github.com/ваш-username/tmc_warehouse.git
```

---

## 4. ЗАГРУЗКА КОДА

### Шаг 4.1: Добавить remote (если новый репозиторий)

```bash
cd /home/meow/work/tmc_warehouse

# Замените URL на ваш!
git remote add origin https://github.com/ваш-username/tmc_warehouse.git
```

**Проверить**:
```bash
git remote -v
```

**Должно показать**:
```
origin  https://github.com/ваш-username/tmc_warehouse.git (fetch)
origin  https://github.com/ваш-username/tmc_warehouse.git (push)
```

---

### Шаг 4.2: Загрузить код на GitHub

```bash
git push -u origin main
```

**Что произойдет**:
- GitHub попросит авторизацию
- **Современный способ (Personal Access Token)**:
  1. Перейти: https://github.com/settings/tokens
  2. "Generate new token" → "Classic"
  3. Выбрать срок действия (например, 90 days)
  4. Отметить scopes: `repo` (весь раздел)
  5. "Generate token"
  6. **Скопировать токен** (показывается один раз!)
  7. Вставить как пароль при `git push`

**Username**: ваш GitHub login  
**Password**: вставить токен (не показывается при вводе — это нормально)

---

### Шаг 4.3: Проверить загрузку

Откройте в браузере:
```
https://github.com/ваш-username/tmc_warehouse
```

**Должны увидеть**:
- ✅ Все файлы проекта
- ✅ `.github/workflows/build-portable.yml`
- ✅ README.md

---

## 5. АКТИВАЦИЯ GITHUB ACTIONS

### Шаг 5.1: Перейти в Actions

```
https://github.com/ваш-username/tmc_warehouse/actions
```

**Возможные варианты**:

#### Вариант А: "Get started with GitHub Actions"
- **Нажать** зеленую кнопку **"I understand my workflows, go ahead and enable them"**

#### Вариант Б: Actions уже активны
- Увидите список workflows
- **Отлично!** Переходите к Шагу 5.2

---

### Шаг 5.2: Проверить что workflow появился

В разделе **Actions** → слева должен появиться:
- ✅ **Build Portable Windows**

**Если не появился**:
1. Проверьте что файл `.github/workflows/build-portable.yml` загружен
2. Обновите страницу (F5)
3. Подождите 1-2 минуты

---

### Шаг 5.3: Запустить workflow вручную (первый раз)

1. **Actions** → слева выбрать **"Build Portable Windows"**
2. Справа нажать **"Run workflow"**
3. Выбрать ветку **"main"**
4. Нажать зеленую кнопку **"Run workflow"**

**Статус**:
- 🟡 Желтый кружок — выполняется
- ✅ Зеленая галочка — успешно
- ❌ Красный крестик — ошибка

---

## 6. ПРОВЕРКА СБОРКИ

### Шаг 6.1: Открыть детали выполнения

**Кликните** на название запущенного workflow (например, "Add portable Windows build workflow")

---

### Шаг 6.2: Следить за прогрессом

Увидите список шагов:
```
✅ Checkout code
🔄 Set up Python (выполняется...)
⏸️ Download Tesseract Portable (ожидает)
⏸️ Build Server EXE (ожидает)
...
```

**Время выполнения**: ~10-15 минут (первая сборка)

---

### Шаг 6.3: Проверить логи (если интересно)

**Кликните** на любой шаг, чтобы увидеть подробные логи.

**Пример успешного лога**:
```
Download Tesseract Portable
  Downloading Tesseract 5.3.3...
  ✓ Tesseract downloaded successfully
  tesseract 5.3.3
```

---

### Шаг 6.4: Дождаться завершения

Когда все шаги станут зелеными ✅:
```
✅ Checkout code
✅ Set up Python
✅ Download Tesseract Portable
✅ Download Poppler Portable
✅ Build Server EXE
✅ Build Client EXE
✅ Create Portable Package
✅ Create ZIP Archive
✅ Upload Portable Package
```

**Сборка завершена!** 🎉

---

## 7. СКАЧИВАНИЕ РЕЗУЛЬТАТА

### Шаг 7.1: Найти артефакты

**После успешной сборки** → прокрутить страницу вниз до раздела **"Artifacts"**

Увидите:
```
📦 TMC-Warehouse-Portable-dev-xxxxxxx
   Size: ~150-200 MB
   Expires in 90 days
```

---

### Шаг 7.2: Скачать ZIP

**Кликните** на название артефакта → начнется скачивание

**Файл**: `TMC-Warehouse-Portable-dev-xxxxxxx.zip`

---

### Шаг 7.3: Протестировать на Windows (рекомендуется)

1. **Распакуйте** ZIP на Windows машине
2. **Двойной клик** на `START_SERVER.bat`
3. **Двойной клик** на `START_CLIENT.bat`
4. **Проверьте** работу приложения

**Если работает** — готово! ✅

---

## 8. СОЗДАНИЕ РЕЛИЗА (опционально)

### Когда создавать релиз?
- Когда версия стабильна и протестирована
- Для удобной загрузки пользователями
- Релизы хранятся **бессрочно** (в отличие от Artifacts — 90 дней)

---

### Шаг 8.1: Создать тег версии

```bash
cd /home/meow/work/tmc_warehouse

# Создать тег (например, v1.0.0)
git tag -a v1.0.0 -m "Release version 1.0.0 - первая portable сборка"

# Отправить тег на GitHub
git push origin v1.0.0
```

---

### Шаг 8.2: Дождаться автоматической сборки

GitHub Actions автоматически:
1. Запустит workflow при обнаружении тега `v*.*.*`
2. Соберет portable версию
3. **Создаст GitHub Release**
4. **Прикрепит ZIP к релизу**

**Время**: ~10-15 минут

---

### Шаг 8.3: Проверить релиз

Перейти:
```
https://github.com/ваш-username/tmc_warehouse/releases
```

Увидите:
```
🎉 TMC Warehouse Portable v1.0.0
   
   📦 Assets:
   - TMC_Warehouse_Portable_v1.0.0_Windows_x64.zip (150-200 MB)
   
   Готово к скачиванию!
```

---

## 📝 КРАТКАЯ ШПАРГАЛКА

### Первоначальная загрузка:
```bash
cd /home/meow/work/tmc_warehouse
git add .
git commit -m "feat: add portable build workflow"
git remote add origin https://github.com/ваш-username/tmc_warehouse.git
git push -u origin main
```

### Последующие обновления:
```bash
git add .
git commit -m "описание изменений"
git push
```

### Создание релиза:
```bash
git tag -a v1.0.0 -m "Release 1.0.0"
git push origin v1.0.0
```

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: "Authentication failed"

**Решение**:
```bash
# Использовать Personal Access Token вместо пароля
# Создать токен: https://github.com/settings/tokens
# При git push вставить токен как пароль
```

---

### Проблема: Workflow не запускается

**Проверить**:
1. Actions включены: Settings → Actions → General → "Allow all actions"
2. Файл workflow в правильном месте: `.github/workflows/build-portable.yml`
3. YAML синтаксис корректен (проверить отступы)

---

### Проблема: Сборка падает с ошибкой

**Шаги**:
1. Открыть логи упавшего шага
2. Найти строку с `ERROR` или `Failed`
3. Скопировать ошибку
4. Гуглить или спросить меня 😊

**Частые ошибки**:
- Неправильная версия Tesseract/Poppler → обновить в `env:` секции
- Не хватает зависимостей → добавить в `requirements.txt`
- PyInstaller не находит модуль → добавить в `--hidden-import`

---

## ✅ ГОТОВО!

После выполнения всех шагов у вас будет:

✅ Код на GitHub  
✅ Автоматическая сборка при каждом push  
✅ Portable версия в Artifacts  
✅ (Опционально) Релизы для скачивания  

**Следующий push** автоматически запустит сборку!

```bash
# Внести изменения в код...
git add .
git commit -m "fix: исправил баг"
git push
# → GitHub Actions автоматически соберет новую версию!
```
