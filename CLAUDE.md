# Claude Code Instructions - TMC Warehouse

## Основной принцип
**ВСЕГДА ГОВОРИ ПО-РУССКИ** | Качество > Скорость | Правильная архитектура > Быстрые костыли

## ⚠️ КРИТИЧЕСКИЙ ПРИНЦИП ПЕРЕД ИЗМЕНЕНИЯМИ
**ПЕРЕД ВНЕСЕНИЕМ ЛЮБЫХ ИЗМЕНЕНИЙ ОБЯЗАТЕЛЬНО:**
1. **🔍 Проверь внутренние противоречия** - убедись что нет конфликтов с другими правилами
2. **🧩 Анализируй совместимость** - убедись что изменения не нарушат работу существующих систем
3. **🎯 Ищи оптимальное решение** - найди наиболее эффективный, надежный и простой способ
4. **🚫 Избегай усложнений** - НЕ создавай избыточное усложнение, костыли, заплатки или запасные варианты
5. **🔧 Минимальное вмешательство** - делай минимально необходимые изменения для решения проблемы

**Принцип валидации изменений:**
- ✅ Решение элегантное и простое?
- ✅ Нет конфликтов с архитектурой?
- ✅ Не добавляет технический долг?
- ✅ Минимальное количество изменений?
- ✅ Не требует fallback решений?

### ⚠️ ВАЖНО: Запрет fallback и костылей
**НЕТ fallback/workaround если не просили явно:**
- НЕ создавай запасные варианты
- НЕ добавляй заплатки автоматически  
- НЕ делай костыли "на всякий случай"
- Либо работает правильно, либо не работает
- Исключение: только если пользователь ЯВНО попросил fallback

---

## 📋 Алгоритм работы

### ШАГ 1: Анализ и роль
- **Тип задачи:** [категория]
- **Сложность:** [1-10]
- **Технологии:** [стек]
- **Роль:** [специализация]

### ШАГ 2: Архитектура
- **Структура:** [компоненты]
- **Взаимодействие:** [схема]
- **Паттерны:** [используемые подходы]

### ШАГ 3: Декомпозиция
**Подзадачи:**
1. [Подзадача 1]
2. [Подзадача 2]
3. [Подзадача N]

**Этапы:**
- Этап 1: [название] - [описание]
- Этап 2: [название] - [описание]
- Этап N: [название] - [описание]

### ШАГ 4: Реализация
[Полный код с подробными комментариями]

### ШАГ 5: Качество
**Чеклист:**
- [ ] Архитектура (BDUF): __/10
- [ ] Простота (KISS): __/10  
- [ ] Без лишнего (YAGNI): __/10
- [ ] Читаемость: __/10
- [ ] Производительность: __/10
- [ ] Типизация (type hints): __/10

**Самокритика:**
- ✅ Получилось: [достижения]
- ⚠️ Можно улучшить: [недостатки]
- 🚨 Риски: [потенциальные проблемы]

---

## 📁 Работа с большими файлами

### Лимиты Claude Code:
- **Read инструмент:** максимум 25K токенов (~2000 строк для Python)
- **При превышении:** файл НЕ читается вообще, нужны альтернативные методы

### Стратегии чтения больших файлов:

#### 1. **Чтение частями через offset/limit**
```python
# Сначала узнать размер
Bash: wc -l filename.py

# Читать по частям (примерно 1000-1500 строк за раз)
Read file_path offset=0 limit=1000      # строки 1-1000
Read file_path offset=1000 limit=1000   # строки 1001-2000
Read file_path offset=2000 limit=1000   # строки 2001-3000
```

#### 2. **Grep для поиска конкретного кода**
```python
# Найти функции/классы
Grep pattern="class\s+\w+|def\s+\w+" glob="*.py"

# Найти конкретную функциональность
Grep pattern="process_document|ocr_service" glob="*.py" path="client/src/"
```

#### 3. **Bash для быстрого анализа структуры**
```bash
# Показать начало файла (структуру, импорты)
head -50 filename.py

# Показать конец файла
tail -50 filename.py

# Показать определения функций
grep -n "def \|class " filename.py

# Показать конкретный диапазон строк
sed -n '100,200p' filename.py
```

#### 4. **Task инструмент для сложного анализа**
```python
# Для детального поиска и анализа без загрузки всего файла
Task("Найти все методы OCR в ocr_service.py")
```

### ⚠️ Правила работы с большими файлами:
- **ВСЕГДА сначала `wc -l`** - узнать размер файла
- **НЕ пытайся Read** файлы >2000 строк без параметров
- **Используй Grep** для поиска перед чтением
- **Читай по смысловым блокам** - импорты, классы, методы отдельно
- **Рефактори большие файлы** на модули при возможности

---

## 🎯 Универсальные принципы

### Принципы разработки:
**KISS** - максимальная простота 
**YAGNI** - только необходимое
**DRY** - без дублирования
**BDUF** - продуманная архитектура 
**SOLID** - правильный ООП

### Правила кода:
- Type hints обязательно
- Говорящие имена переменных
- Комментарии для сложной логики
- Единый стиль форматирования (PEP 8)

### 📁 Правила именования файлов:
⚠️ **КРИТИЧНЫЕ правила именования:**
- **НЕ добавлять суффиксы** `-fixed`, `-new`, `-updated`, `-v2` и подобные к именам файлов
- **НЕ создавать дублирующие файлы** с модификаторами в названии
- **ВСЕГДА редактировать существующие файлы** вместо создания новых с похожими именами
- **Использовать осмысленные имена** без временных маркеров
- **При необходимости рефакторинга** переименовывать оригинальный файл, а не создавать копию

---

## 🏗️ Архитектура проекта TMC Warehouse

### Структура проекта:
```
tmc_warehouse/
├── server/              # Серверная часть (FastAPI + Peewee)
│   └── src/
│       ├── api/         # REST API роутеры
│       ├── db/          # ORM модели и репозитории
│       ├── config.py    # Конфигурация сервера
│       └── main_server.py
├── client/              # Клиентская часть (PySide6)
│   └── src/
│       ├── ui/          # UI диалоги и виджеты
│       ├── services/    # Бизнес-логика (OCR, камера, синхронизация)
│       ├── config.py    # Конфигурация клиента
│       └── main_client.py
├── common/              # Общие Pydantic модели
│   └── models.py        # ⚠️ НЕ ИЗМЕНЯТЬ! Эталонные модели
├── config/              # Конфигурационные файлы
├── data/                # База данных и файлы приёмок
├── docs/                # Документация
└── tests/               # Тесты
```

### Технологический стек:
- **Backend:** Python 3.12, FastAPI, Uvicorn, Peewee, SQLite
- **Frontend:** PySide6 (Qt)
- **OCR:** Tesseract, pytesseract, pdf2image, pdfplumber
- **Камера:** OpenCV (cv2)
- **HTTP:** requests
- **Валидация:** Pydantic

### Основные компоненты:

#### Сервер (server/src/):
- **API роутеры:**
  - `routes_health.py` - Health check
  - `routes_products.py` - Справочник товаров
  - `routes_receptions.py` - Приёмки ТМЦ
  - `routes_files.py` - Загрузка документов и видео
  - `routes_downloads.py` - Скачивание файлов
- **База данных:**
  - `db/models.py` - Peewee ORM модели
  - `db/repository.py` - CRUD операции
  - `db/migrations.py` - Создание таблиц

#### Клиент (client/src/):
- **Сервисы:**
  - `ocr_service.py` - Распознавание ТТН
  - `camera_service.py` - Работа с веб-камерой
  - `sync_service.py` - HTTP клиент к серверу
  - `validator_service.py` - Валидация входного контроля
  - `storage_service.py` - Локальное хранение
- **UI диалоги:**
  - `main_window.py` - Главное окно
  - `document_dialog.py` - Загрузка и OCR документа
  - `results_widget.py` - Таблица позиций
  - `history_dialog.py` - История приёмок
  - `reception_detail_dialog.py` - Детали приёмки
  - `database_dialog.py` - Справочник товаров
  - `video_widget.py` - Превью камеры
  - `control_dialog.py` - Входной контроль
  - `settings_dialog.py` - Настройки
  - `styles.py` - QSS стили

#### Общие модели (common/models.py):
⚠️ **КРИТИЧНО:** Эти модели являются эталоном, НЕ ИЗМЕНЯЙ их без явной необходимости!
- `ProductCreate`, `ProductRead` - Товары
- `ReceptionCreate`, `ReceptionRead`, `ReceptionShort` - Приёмки
- `ReceptionItemCreate`, `ReceptionItemRead` - Позиции приёмки
- `ReceptionItemControlUpdate` - Обновление контроля
- `OCRResult`, `OCRItem` - Результаты OCR
- `ValidationResult` - Результаты валидации
- Enums: `ReceptionStatus`, `ControlStatus`, `ControlType`

---

## 🚀 Правила запуска сервера

**Корректные команды запуска:**

1. **Основной запуск (из корня проекта):**
```bash
# Linux/Mac
./run_server.sh

# Windows
run_server.bat
```

2. **Ручной запуск:**
```bash
source .venv/bin/activate  # или .venv\Scripts\activate на Windows
python server/src/main_server.py
```

3. **Тестовый запуск с таймаутом:**
```bash
timeout 10 bash -c "source .venv/bin/activate && python server/src/main_server.py"
```

**⚠️ КРИТИЧЕСКИ ВАЖНЫЕ правила:**
- **ВСЕГДА** запускать из корневой директории проекта
- **НЕ** запускать из директории server/
- **🚫 АБСОЛЮТНО ЗАПРЕЩЕНО:** Останавливать, перезапускать или запускать сервер самостоятельно
- **✅ ПРАВИЛЬНО:** Сообщать пользователю о необходимости перезапуска сервера
- **⚠️ НИКОГДА НЕ ИСПОЛЬЗОВАТЬ:** команды `pkill`, `kill`, `systemctl restart`, `service restart` и подобные
- **📢 ВМЕСТО ЭТОГО:** "Нужно перезапустить сервер для применения изменений"

---

## 🧪 Тестирование

### Запуск тестов:
```bash
# Все тесты
pytest

# Тесты базы данных
pytest tests/test_server_db.py

# Тесты API
pytest tests/test_server_api.py

# UI тесты
python tests/test_ui_components.py
```

### Типы тестов:
- **Unit тесты:** `test_models.py`, `test_server_db.py`
- **Integration тесты:** `test_server_api.py`
- **UI тесты:** `test_ui_components.py`, `test_client_startup.py`

---

## 📚 Документация проекта

### Основные документы:
- **README.md** - Быстрый старт и обзор
- **docs/ARCHITECTURE.md** - Архитектура системы
- **docs/API.md** - Спецификация REST API
- **docs/TMC_WAREHOUSE_PROMPT_V2.md** - Подробный план разработки (для ИИ)
- **docs/USER_GUIDE.md** - Руководство пользователя
- **docs/INSTALL.md** - Инструкция по установке

### ⚠️ ВАЖНО: Перед началом работы
**ОБЯЗАТЕЛЬНО изучай соответствующую документацию:**
1. Для backend работы → `docs/API.md`
2. Для клиента → `docs/TMC_WAREHOUSE_PROMPT_V2.md`
3. Для архитектурных изменений → `docs/ARCHITECTURE.md`

---

## 📝 ПРАВИЛА СОЗДАНИЯ ДОКУМЕНТАЦИИ

### ⚠️ НЕ СОЗДАВАТЬ документацию без запроса:
- **НЕ пиши README файлы** автоматически
- **НЕ создавай инструкции** если не попросили
- **НЕ делай руководства** по использованию
- **НЕ генерируй гайды** без явного запроса

### ✅ КОГДА можно создать документацию:
- Пользователь **явно попросил** написать инструкцию
- Пользователь спросил **"как использовать"** или **"напиши руководство"**
- Пользователь сказал **"создай документацию"**

### 🎯 Принцип: **ДЕЛАЙ ТОЛЬКО ТО, ЧТО ПРОСЯТ**
- Решил проблему → стоп
- Написал код → стоп  
- Исправил баг → стоп
- **НЕ добавляй** "для удобства" документацию

---

## 🔧 Специфические правила для TMC Warehouse

### Работа с OCR:
- **Tesseract** должен быть установлен в системе
- **Poppler** требуется для работы с PDF
- Пути к Tesseract и Poppler указаны в `config/config.json`
- Языки распознавания: `rus` + `eng`
- Предобработка: grayscale → adaptive threshold → median blur

### Работа с камерой:
- Использовать `cv2.VideoCapture` через `CameraService`
- Запись в формате AVI с кодеком MJPG
- Превью через Qt signals (QImage)
- Многопоточность через `QThread`

### Работа с базой данных:
- **SQLite** с Peewee ORM
- **WAL режим** для параллельного доступа
- Каскадное удаление связанных записей
- Все изменения через `repository.py`

### Хранение файлов:
- Структура: `data/receipts/YYYY-MM-DD_<id>/`
- Документы: `document.pdf` или `document.jpg`
- Видео: `video.avi`
- Пути в БД: относительные от `data/`

### API эндпоинты:
```
GET  /api/v1/health
GET  /api/v1/products
GET  /api/v1/products/{article}
POST /api/v1/receptions
GET  /api/v1/receptions
GET  /api/v1/receptions/{id}
POST /api/v1/receptions/{id}/document
POST /api/v1/receptions/{id}/video
POST /api/v1/receptions/{id}/control-results
GET  /api/v1/receptions/{id}/document
GET  /api/v1/receptions/{id}/video
```

### Рабочий процесс:
1. **Загрузка документа** → OCR распознавание
2. **Проверка данных** → редактирование подозрительных полей
3. **Создание приёмки** → отправка на сервер
4. **Входной контроль** → видеофиксация (если требуется)
5. **Завершение** → обновление статуса

---

## 🎯 Помни

**Качество > Скорость | Архитектура > Костыли | Простота > Сложность**
