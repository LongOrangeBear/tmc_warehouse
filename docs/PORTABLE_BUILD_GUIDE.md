# 🚀 Как использовать Portable Build

## Автоматизация готова! ✅

Я создал `.github/workflows/build-portable.yml` — полностью автоматический pipeline для Portable сборки.

---

## 📋 Что делает workflow:

1. **Скачивает Tesseract** (~50MB) с официального сайта UB-Mannheim
2. **Скачивает Poppler** (~10MB) с GitHub Releases  
3. **Собирает EXE** через PyInstaller
4. **Упаковывает всё** в структуру:
   ```
   TMC_Warehouse_Portable/
   ├── tmc_server.exe
   ├── tmc_client.exe
   ├── START_SERVER.bat      ← Простой запуск
   ├── START_CLIENT.bat
   ├── Tesseract-OCR/        ← Встроено!
   ├── poppler/              ← Встроено!
   ├── config/
   │   └── config.json       ← Настроено для portable
   └── data/
   ```
5. **Создает ZIP** (~150-200 MB)
6. **Загружает в GitHub** (Artifacts или Release)

---

## 🎯 Как запустить:

### Вариант 1: Вручную (для теста)

```bash
# В вашем репозитории на GitHub:
Actions → Build Portable Windows → Run workflow → Run
```

### Вариант 2: Автоматически при push

```bash
git add .
git commit -m "feat: enable portable build"
git push origin main
```

### Вариант 3: Создать релиз

```bash
git tag -a v1.0.0 -m "First portable release"
git push origin v1.0.0
```

---

## 📥 Где скачать результат:

### После успешной сборки:

1. **GitHub Actions**:
   ```
   Actions → Build Portable Windows → 
   Последний успешный запуск → 
   Artifacts → TMC-Warehouse-Portable-xxx
   ```

2. **GitHub Releases** (если создавали тег):
   ```
   Releases → Latest release → 
   TMC_Warehouse_Portable_vX.X.X_Windows_x64.zip
   ```

---

## ⏱️ Время сборки:

- **Первая сборка**: ~10-15 минут (скачивание Tesseract/Poppler)
- **Последующие**: ~5-7 минут (кеширование)

---

## 🔍 Что внутри ZIP архива:

Пользователь получает:
1. **Скачивает один ZIP** (~150-200 MB)
2. **Распаковывает**
3. **Двойной клик** по `START_SERVER.bat`
4. **Двойной клик** по `START_CLIENT.bat`
5. **Готово!** Всё работает без установки

---

## ⚠️ Важные моменты:

### Лицензии
- **Tesseract**: Apache 2.0 (можно распространять)
- **Poppler**: GPL v2 (можно распространять, но нужно указать лицензию)
- Оба включены в README.txt автоматически

### Размер архива
- **~150-200 MB** (зависит от версий Tesseract/Poppler)
- Это нормально для portable приложений с OCR

### GitHub Artifacts
- Хранятся **90 дней** (можно изменить в workflow)
- Releases хранятся **бессрочно**

---

## 🐛 Troubleshooting

### Если сборка падает:

1. **Проверьте версии**:
   ```yaml
   TESSERACT_VERSION: '5.3.3'  # Обновите если нужно
   POPPLER_VERSION: '24.02.0'
   ```

2. **Проверьте логи**:
   ```
   Actions → Failed run → Кликните на шаг с ошибкой
   ```

3. **Локальный тест** (опционально):
   ```bash
   # На Windows машине:
   pip install pyinstaller
   pyinstaller --onefile client/src/main_client.py
   ```

---

## ✅ Готово к использованию!

Просто сделайте:
```bash
git add .github/workflows/build-portable.yml
git commit -m "feat: add portable build workflow"
git push origin main
```

И ждите ~10 минут. Portable сборка появится в Artifacts!
