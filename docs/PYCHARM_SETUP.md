# PyCharm - Запуск через клик

## ✅ Готовые конфигурации созданы!

Я создал 3 готовых Run Configuration в папке `.run/`:
- **Server.run.xml** - запуск сервера
- **Client.run.xml** - запуск клиента
- **Tests - All.run.xml** - запуск всех тестов

PyCharm автоматически подхватит их при следующем открытии проекта.

---

## 🚀 Как использовать:

### 1. Перезапусти PyCharm
Закрой и открой проект заново, чтобы PyCharm увидел новые конфигурации.

### 2. Выбери конфигурацию
В правом верхнем углу PyCharm:
- Увидишь dropdown с конфигурациями
- Выбери **"Server"** или **"Client"**

### 3. Запусти одним кликом
- Нажми зелёный треугольник ▶️ (Run)
- Или `Shift + F10`

---

## 📋 Доступные конфигурации:

### 🖥️ Server
- Запускает `server/src/main_server.py`
- Автоматически инициализирует БД
- Сервер стартует на `http://127.0.0.1:8000`
- PYTHONPATH настроен правильно

### 💻 Client  
- Запускает `client/src/main_client.py`
- Открывает GUI приложение
- PYTHONPATH настроен правильно
- QT_QPA_PLATFORM = xcb (для Linux GUI)

### 🧪 Tests - All
- Запускает все тесты в `tests/`
- Verbose output (`-v`)
- QT_QPA_PLATFORM = offscreen (для headless UI тестов)

---

## 🔧 Ручная настройка (если автоматически не подхватилось):

1. **Открой:** Run → Edit Configurations
2. **Нажми:** ➕ (Add New Configuration)
3. **Выбери:** Python
4. **Заполни:**

### Для Server:
```
Name: Server
Script path: /home/meow/work/tmc_warehouse/server/src/main_server.py
Working directory: /home/meow/work/tmc_warehouse
Python interpreter: .venv/bin/python
Environment variables: PYTHONPATH=/home/meow/work/tmc_warehouse
```

### Для Client:
```
Name: Client
Script path: /home/meow/work/tmc_warehouse/client/src/main_client.py
Working directory: /home/meow/work/tmc_warehouse
Python interpreter: .venv/bin/python
Environment variables: 
  PYTHONPATH=/home/meow/work/tmc_warehouse
  QT_QPA_PLATFORM=xcb
```

---

## 🎯 Быстрый старт для тестирования:

1. **Запусти Server:**
   - Выбери "Server" в dropdown
   - Нажми ▶️
   - Дождись `INFO: Uvicorn running on http://127.0.0.1:8000`

2. **Запусти Client (в новой вкладке):**
   - Выбери "Client" в dropdown
   - Нажми ▶️
   - Откроется GUI

3. **Тестируй:**
   - В клиенте нажми "Принять ТМЦ"
   - Выбери PDF/изображение
   - Нажми "Распознать"
   - Создай приёмку

---

## 💡 Советы:

### Логи в PyCharm:
- Все логи видны в панели "Run" внизу
- Цветные уровни (INFO - зеленый, ERROR - красный)
- Можно кликать на файлы в traceback → переход к коду

### Debug режим:
- Вместо ▶️ нажми 🐞 (Debug)
- Ставь breakpoints в коде
- Пошаговая отладка работает

### Остановка:
- Красный квадрат ⏹️ (Stop)
- Или `Ctrl + F2`

---

## ✅ Проверка что работает:

После запуска Server должен вывести:
```
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://127.0.0.1:8000
```

После запуска Client должно открыться окно с тремя кнопками.

**Готово к тестированию!** 🚀
