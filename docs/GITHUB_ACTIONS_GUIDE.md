# 🚀 GitHub Actions: Автоматическая сборка и CI

## Обзор

В проекте настроено **два независимых workflow**:

1.  **TMC Warehouse CI** (`.github/workflows/ci.yml`)
    *   **Цель**: Быстрая проверка кода (тесты).
    *   **Когда запускается**: При каждом `git push` или Pull Request.
    *   **Где выполняется**: Linux (Ubuntu).
    *   **Что делает**: Устанавливает зависимости, запускает линтеры и тесты (`pytest`).

2.  **Build Portable Windows** (`.github/workflows/build-portable.yml`)
    *   **Цель**: Создание готового к распространению `.exe` пакета.
    *   **Когда запускается**:
        *   При пуше в ветку `master` или `main`.
        *   При создании тега версии (например, `v1.0.0`).
        *   Вручную (кнопка "Run workflow").
    *   **Где выполняется**: Windows.
    *   **Что делает**:
        *   Собирает `tmc_server.exe` и `tmc_client.exe`.
        *   Скачивает и внедряет **Portable Tesseract OCR** и **Poppler**.
        *   Создает ZIP-архив со всем необходимым для запуска на любом ПК.

---

## 1. TMC Warehouse CI (Тестирование)

Этот процесс гарантирует, что новый код не ломает существующий функционал.

### Статусы
*   ✅ **Green**: Все тесты прошли. Можно мержить.
*   ❌ **Red**: Ошибка в тестах. Смотрите логи в Actions -> TMC Warehouse CI.

---

## 2. Build Portable Windows (Сборка)

Это основной процесс для получения "боевой" версии программы.

### Как получить сборку (Artifacts)

1.  Зайдите во вкладку **Actions** на GitHub.
2.  Выберите слева **Build Portable Windows**.
3.  Нажмите на последний успешный (зеленый) запуск.
4.  Прокрутите вниз до раздела **Artifacts**.
5.  Скачайте файл `TMC-Warehouse-Portable-...`.

### Что внутри архива?
Это **полностью портативная** версия. Её можно распаковать на флешку или любой компьютер с Windows 10/11, и она будет работать сразу.

*   `START_SERVER.bat` — запуск сервера.
*   `START_CLIENT.bat` — запуск клиента.
*   `config/` — настройки.
*   `data/` — база данных и файлы.
*   `Tesseract-OCR/` — встроенный движок распознавания текста.
*   `poppler/` — встроенная библиотека для PDF.

Вам **НЕ НУЖНО** устанавливать Python, Tesseract или Poppler на целевом компьютере. Всё уже включено.

---

## 3. Локальная сборка (Альтернатива)

Если вы хотите собрать `.exe` на своем компьютере (без GitHub), используйте скрипт `build.py`.

**Требования:**
*   Windows 10/11
*   Python 3.12
*   Установленный Git

**Запуск:**
```bash
python build.py
```

*Примечание: Локальный скрипт `build.py` создает простую сборку. Он НЕ скачивает portable версии Tesseract/Poppler автоматически, как это делает GitHub Action. Для локальной сборки эти программы должны быть уже установлены в системе.*
├── .gitignore
└── README.md
```

---

## ✅ ЧЕКЛИСТ НАСТРОЙКИ

- [ ] Создана папка `.github/workflows/`
- [ ] Создан файл `ci.yml`
- [ ] Создан `requirements-dev.txt`
- [ ] Есть хотя бы один тест в `tests/`
- [ ] Репозиторий на GitHub
- [ ] Первый `git push` выполнен
- [ ] Actions запустился (проверить во вкладке Actions)
- [ ] Тесты проходят (зелёная галочка)
- [ ] Артефакты генерируются (для main ветки)

---

**Готово!** Теперь каждый push будет автоматически тестироваться, а push в main — собирать exe.
