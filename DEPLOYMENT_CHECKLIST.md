# 📋 Checklist: Подготовка к Windows Build через GitHub Actions

## ✅ Что уже готово

### 1. **Документация**
- ✅ `GITHUB_ACTIONS_GUIDE.md` — полное руководство по настройке CI/CD
- ✅ `requirements.txt` — основные зависимости
- ✅ Существующий workflow `.github/workflows/ci.yml` (базовый)

### 2. **Структура проекта**
- ✅ Модульная архитектура (`common/`, `server/`, `client/`)
- ✅ Конфигурация `config/config.json`
- ✅ Скрипты запуска (`run_server.bat`, `run_client.bat`)

---

## ⚠️ Что нужно добавить

### 1. **PyInstaller Spec файлы** (опционально, но рекомендуется)
Создать `scripts/build_server.spec` и `scripts/build_client.spec` по примеру из `GITHUB_ACTIONS_GUIDE.md`

### 2. **Иконки приложений**
Создать или найти:
- `resources/icons/server.ico` — иконка для сервера
- `resources/icons/client.ico` — иконка для клиента

**Примечание**: Если иконок нет, PyInstaller использует стандартную.

### 3. **requirements-dev.txt**
Создать файл с тестовыми зависимостями:
```txt
-r requirements.txt

# Testing
pytest==8.0.0
pytest-cov==4.1.0
httpx==0.26.0

# Build
pyinstaller==6.3.0
```

### 4. **Обновить CI/CD Workflow**
Заменить `.github/workflows/ci.yml` на полный из `GITHUB_ACTIONS_GUIDE.md` (включая джоб `build-windows` и `release`)

---

## 📦 Что нужно на целевой Windows машине

### **ОБЯЗАТЕЛЬНО установить:**

#### 1. **Tesseract OCR** (КРИТИЧНО!)
❌ **НЕ включается в EXE** (внешняя зависимость)

**Установка:**
- Скачать: https://github.com/UB-Mannheim/tesseract/wiki
- Версия: 5.x
- **Обязательно** отметить русский язык при установке
- Путь по умолчанию: `C:\Program Files\Tesseract-OCR`

**Конфигурация:**
```json
{
  "tesseract": {
    "path": "C:/Program Files/Tesseract-OCR/tesseract.exe"
  }
}
```

#### 2. **Poppler** (для PDF → изображения)
❌ **НЕ включается в EXE** (бинарные утилиты)

**Установка:**
- Скачать: https://github.com/oswindows/poppler-windows/releases
- Распаковать в `C:\poppler`
- Добавить в PATH: `C:\poppler\bin`

**Конфигурация:**
```json
{
  "poppler": {
    "path": "C:/poppler/bin"
  }
}
```

---

### **Включается в EXE автоматически:**

✅ Python 3.12 (встроен в exe через PyInstaller)
✅ PySide6 (Qt GUI)
✅ FastAPI + Uvicorn (сервер)
✅ OpenCV (cv2)
✅ NumPy, Pillow
✅ requests, pydantic, peewee
✅ pytesseract (Python обёртка)
✅ pdf2image (Python обёртка)

**Размер финального EXE:**
- `tmc_server.exe`: ~30-50 MB
- `tmc_client.exe`: ~80-120 MB (из-за PySide6 + OpenCV)

---

## 🚀 Инструкция для конечного пользователя

### **Шаг 1: Скачать релиз**
```
GitHub → Releases → tmc_warehouse_v1.0.0.zip
```

### **Шаг 2: Распаковать**
```
C:\TMC_Warehouse\
├── tmc_server.exe
├── tmc_client.exe
├── run_server.bat
├── run_client.bat
├── config\
│   └── config.json
├── data\       (создается автоматически)
└── README.txt
```

### **Шаг 3: Установить зависимости**
1. **Tesseract OCR**:
   - https://github.com/UB-Mannheim/tesseract/wiki
   - При установке выбрать русский язык
   
2. **Poppler**:
   - https://github.com/oswindows/poppler-windows/releases
   - Распаковать и добавить в PATH

### **Шаг 4: Настроить config/config.json**
```json
{
  "tesseract": {
    "path": "C:/Program Files/Tesseract-OCR/tesseract.exe"
  },
  "poppler": {
    "path": "C:/poppler/bin"
  }
}
```

### **Шаг 5: Запуск**
```
1. Двойной клик по run_server.bat (оставить окно открытым)
2. Двойной клик по run_client.bat
```

---

## 🛠️ Альтернатива: Портативная сборка

Если нужна **полностью portable** версия (без установки Tesseract/Poppler):

### **Вариант 1: Включить в архив**
```
tmc_warehouse_portable/
├── tmc_server.exe
├── tmc_client.exe
├── Tesseract-OCR/       ← Скопировать из C:\Program Files\
│   ├── tesseract.exe
│   └── tessdata/
│       └── rus.traineddata
├── poppler/             ← Скопировать бинарники
│   └── bin/
│       ├── pdftoppm.exe
│       └── ...
├── config/
│   └── config.json      ← Относительные пути
└── run.bat
```

**config.json:**
```json
{
  "tesseract": {
    "path": "./Tesseract-OCR/tesseract.exe"
  },
  "poppler": {
    "path": "./poppler/bin"
  }
}
```

**Размер архива:** ~150-200 MB

### **Вариант 2: Installer (NSIS/Inno Setup)**
Создать Windows installer, который:
1. Распакует EXE
2. Автоматически скачает/установит Tesseract и Poppler
3. Настроит пути в конфиге

---

## 📝 Checklist перед первым билдом

- [ ] Создать `requirements-dev.txt`
- [ ] Обновить `.github/workflows/ci.yml` (взять из GUIDE)
- [ ] (Опционально) Создать `.spec` файлы для PyInstaller
- [ ] (Опционально) Добавить иконки `.ico`
- [ ] Протестировать локально:
  ```bash
  pip install pyinstaller
  pyinstaller --onefile server/src/main_server.py
  pyinstaller --onefile --windowed client/src/main_client.py
  ```
- [ ] Сделать коммит в `main`
- [ ] Проверить Actions → Build успешен
- [ ] Скачать артефакт и протестировать на чистой Windows VM
- [ ] Создать тег `v1.0.0` для первого релиза

---

## 🎯 Ответ на вопрос

### **Достаточно ли наших EXE?**
❌ **НЕТ**. На целевой Windows машине обязательно нужны:
1. **Tesseract OCR** (для распознавания текста)
2. **Poppler** (для работы с PDF)

### **Зачем они нужны?**
- PyInstaller может запаковать **Python код** в EXE
- Но **НЕ может** запаковать **внешние бинарные программы** (`tesseract.exe`, `pdftoppm.exe`)
- Это ограничение PyInstaller и стандартная практика

### **Варианты решения:**
1. **Простой**: Пользователь устанавливает Tesseract и Poppler отдельно (5 минут)
2. **Продвинутый**: Создать Portable версию со всем в архиве (~200MB)
3. **Профессиональный**: Создать Windows Installer (NSIS/Inno Setup)
